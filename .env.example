# Copy this to .env.local and fill in values — never commit .env.local

# Apps Script Web App URL (set after deploying the bridge script)
VITE_APPS_SCRIPT_URL=

# Shared secret — must match the value in the Apps Script
VITE_API_SECRET=
